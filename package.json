{
  "name": "slash-ghost-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "start": "npm run start -w server",
    "lint": "npm run lint -w shared && npm run lint -w server && npm run lint -w client"
  }
}
