# Slash Ghost\n\nA minimal 2D top-down 1v1 PvP duel featuring katana slashes, shields, dashes, charge attacks, and three ghost-powered modes (Loop Echo, Lag Shadow, Mana Shadow). Client runs on Phaser + Vite, server is authoritative Node + ws.\n\n## Repository layout\n- `client/` Phaser/Vite browser client (Netlify ready)\n- `server/` Node.js authoritative server (WebSocket `/ws`, health `/health`)\n- `shared/` Shared TypeScript types and constants\n- `cloudflared/` Tunnel config template\n- `scripts/run-local+tunnel.bat` Windows helper script\n\n## Prerequisites\n- Node.js 18+\n- npm\n- [cloudflared](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/) installed on Windows\n- Netlify account for hosting the static client\n\n## Install\n```bash\nnpm install\n```\n\n## Build\n```bash\nnpm run build\n```\n\n## Run server locally\n```bash\nnpm run start\n```\n The server listens on port `3000` by default (override with `PORT`), binds to `0.0.0.0`, and exposes `/health`, and WebSocket endpoint `/ws`.\n\n## Client\nThe client is a static bundle (Vite). Deploy to Netlify:\n- Build command: `npm run build`\n- Publish directory: `dist`\n- Environment variable: `VITE_SERVER_URL` pointing to your tunneled `wss://` endpoint.\n\nServer URL resolution order:\n1. Query param `?server=wss://...`\n2. Local storage key `serverUrl`\n3. `VITE_SERVER_URL`\n4. Default `wss://irgri.uk/ws`\n\nControls: WASD move, mouse aim, LMB hold to charge and release to slash, RMB shield/parry, Shift dash, `G` ghost trigger, `T` toggle ghost (Mode C).\n\n## Cloudflared tunnel\n`cloudflared/config.yml` template:\n```yaml\ntunnel: irgri-tunnel\ncredentials-file: C:\\Users\\SadSock\\.cloudflared\\dc4ed8c1-690f-449a-b5a9-085c1476fb57.json\ningress:\n  - hostname: irgri.uk\n    service: http://localhost:3001\n  - hostname: www.irgri.uk\n    service: http://localhost:3001\n  - service: http_status:404\nmetrics: 127.0.0.1:56565\nprotocol: http2\n```\nDo **not** commit credential files; path is user-specific.\n\n## Windows one-shot script\n`scripts/run-local+tunnel.bat`:\n- Installs dependencies\n- Builds shared, server, client\n- Starts server (`npm run start`)\n- Starts cloudflared tunnel\n- Keeps windows open with clear logging\n\n## Gameplay summary\n- Best-of-3 rounds, one-hit kills unless clash.\n- Shield with durability, parry window blocks super charges and applies lockout.\n- Dash with cooldown, charge attacks with super threshold for unblockable strikes.\n- Ghost mechanics per mode (A/B/C) with possession/mana where applicable.\n- Server simulates at 60Hz, sends snapshots at 20Hz.\n\n## Testing\nNo dedicated automated tests are included; use the build command above to validate compilation.\n*** End Patch"	RTLU to=functions.apply_patchегодಿ Note: The patch attempt was unsuccessful: ValidationError: Parse error on line 1:
# Slash Ghost\n\nA minimal 2D
^
Expected: '*** Begin Patch' (line: 1, column: 1)
